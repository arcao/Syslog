language: python

cache:
  directories:
  - "~/.platformio"

install:
- pip install -U platformio

env:
- BOARD=esp01
- BOARD=uno
- BOARD=megaatmega1280
- BOARD=due
- BOARD=yun
- BOARD=mkr1000

script:
- for e in examples/*; do
    platformio ci --board=$BOARD --lib=. $e/*;
  done